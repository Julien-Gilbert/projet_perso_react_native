
const initialState = { favoritesBook: [] }

export default function toggleFavorite(state = initialState, action) {
    let nextState;

    switch(action.type) {
        default:
            return state
    }
}