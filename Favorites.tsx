import React, { Component } from "react"
import { StyleSheet, Text, TextInput, View, StatusBar, FlatList } from 'react-native'

export default class favorites extends Component {
    render() {
        return(
            <Text>Liste des livres favoris</Text>
        )
    }
}